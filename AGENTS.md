# Codex Instructions

When working in this workspace, treat the Markdown documentation in
`https://github.com/swc9803/hermes` as required project context.

Before answering questions about Hermes setup, configuration, behavior,
installation, troubleshooting, or docs:

1. Read the relevant `.md` files from `swc9803/hermes`.
2. Base the answer on those docs when they apply.
3. If the remote docs cannot be accessed, say that clearly before giving a
   best-effort answer.

Prefer checking the docs first over relying on memory.
